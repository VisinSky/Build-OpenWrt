name: 编译 FanchmWrt

on:
  repository_dispatch:
  workflow_dispatch:
permissions:
  contents: write
  actions: write
env:
  REPO_URL: https://github.com/fanchmwrt/fanchmwrt/
  REPO_BRANCH: fanchmwrt-24.10.4
  FEEDS_CONF: FanchmWrt/feeds.conf.default
  CONFIG_FILE_X86: FanchmWrt/fanchmwrtamd64.config
  CONFIG_FILE_AX3600: FanchmWrt/fanchmwrtax3600.config
  DIY_P1_SH: FanchmWrt/diy-part1.sh
  DIY_P2_SH: FanchmWrt/diy-part2.sh
  PLUGINS_FILE: FanchmWrt/plugins.txt
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 检测(Checkout)
      uses: actions/checkout@v4

    - name: 初始化环境(Initialization environment)
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: 克隆源代码(Clone source code)
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH fanchmwrt
        ln -sf /workdir/fanchmwrt $GITHUB_WORKSPACE/fanchmwrt

    - name: 加载自定义订阅源(Load custom feeds)
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF fanchmwrt/feeds.conf.default
        # 给diy-part1.sh添加可执行权限（确保执行）
        if [ -e $DIY_P1_SH ]; then
          chmod +x $DIY_P1_SH
          echo "✅ 已给$DIY_P1_SH添加可执行权限"
        else
          echo "⚠️ $DIY_P1_SH文件不存在，跳过权限设置"
        fi
        cd fanchmwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: 更新软件源(Update feeds)
      run: cd fanchmwrt && ./scripts/feeds update -a

    - name: 安装软件源(Install feeds)
      run: cd fanchmwrt && ./scripts/feeds install -a

    - name: 生成动态变量（编译时间/Release标签）
      id: gen_vars
      run: |
        COMPILE_TIME=$(date +"%Y-%m-%d %H:%M:%S")
        RELEASE_TAG=$(date +"%Y.%m.%d-%H%M")
        echo "COMPILE_TIME=$COMPILE_TIME" >> $GITHUB_ENV
        echo "RELEASE_TAG=$RELEASE_TAG" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        echo "✅ 生成的编译时间：$COMPILE_TIME"
        echo "✅ 生成的Release标签：$RELEASE_TAG"

    # ===================== 第一步：编译X86/64固件 =====================
    - name: 加载X86/64自定义配置(Load X86/64 config)
      run: |
        [ -e $CONFIG_FILE_X86 ] && mv $CONFIG_FILE_X86 fanchmwrt/.config || echo "⚠️ X86/64配置文件不存在，使用默认配置"
        # 给diy-part2.sh添加可执行权限（核心修复点）
        if [ -e $DIY_P2_SH ]; then
          chmod +x $DIY_P2_SH
          echo "✅ 已给$DIY_P2_SH添加可执行权限"
        else
          echo "⚠️ $DIY_P2_SH文件不存在，跳过权限设置"
        fi
        cd fanchmwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: 提取X86/64固件默认管理IP
      id: extract_ip_x86
      run: |
        cd fanchmwrt
        MANAGE_IP_X86=""
        CONFIG_GENERATE="package/base-files/files/bin/config_generate"
        if [ -f "$CONFIG_GENERATE" ]; then
          MANAGE_IP_X86=$(grep -oP '192\.168\.\d+\.\d+' "$CONFIG_GENERATE" | head -1)
        fi
        if [ -z "$MANAGE_IP_X86" ] && [ -f "files/etc/config/network" ]; then
          MANAGE_IP_X86=$(grep -A 10 'config interface 'lan'' "files/etc/config/network" | grep 'option ipaddr' | sed -r 's/.*ipaddr[[:space:]]*"(.*)"/\1/' | head -1)
        fi
        MANAGE_IP_X86=${MANAGE_IP_X86:-"192.168.1.1"}
        echo "MANAGE_IP_X86=$MANAGE_IP_X86" >> $GITHUB_ENV
        echo "✅ X86/64管理IP：$MANAGE_IP_X86"

    - name: 下载X86/64软件包(Download X86/64 package)
      run: |
        cd fanchmwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 记录X86/64编译开始时间
      id: start_x86
      run: |
        COMPILE_START_X86=$(date +%s)
        echo "COMPILE_START_X86=$COMPILE_START_X86" >> $GITHUB_ENV
        echo "✅ X86/64编译开始时间戳：$COMPILE_START_X86"

    - name: 编译X86/64固件(Compile X86/64 firmware)
      id: compile_x86
      run: |
        cd fanchmwrt
        echo -e "$(nproc) thread compile X86/64"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        
        # 提取X86/64设备信息
        DEVICE_X86=$(grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/')
        ARCH_X86=$(grep '^CONFIG_TARGET_ARCH_PACKAGES=' .config | sed -r 's/.*="(.*)"/\1/')
        FULL_DEVICE_X86="${DEVICE_X86} ${ARCH_X86}"
        echo "FULL_DEVICE_X86=$FULL_DEVICE_X86" >> $GITHUB_ENV
        echo "✅ X86/64设备信息：$FULL_DEVICE_X86"

    - name: 计算X86/64编译时长
      id: calc_x86_duration
      if: steps.compile_x86.outputs.status == 'success' && !cancelled()
      run: |
        COMPILE_END_X86=$(date +%s)
        DURATION_SEC_X86=$((COMPILE_END_X86 - COMPILE_START_X86))
        HOURS_X86=$((DURATION_SEC_X86 / 3600))
        MINUTES_X86=$(((DURATION_SEC_X86 % 3600) / 60))
        SECONDS_X86=$((DURATION_SEC_X86 % 60))
        COMPILE_DURATION_X86=$(printf "%02d:%02d:%02d" $HOURS_X86 $MINUTES_X86 $SECONDS_X86)
        echo "COMPILE_DURATION_X86=$COMPILE_DURATION_X86" >> $GITHUB_ENV
        echo "✅ X86/64编译耗时：$COMPILE_DURATION_X86"

    - name: 备份X86/64固件(Backup X86/64 firmware)
      if: steps.compile_x86.outputs.status == 'success' && !cancelled()
      run: |
        # 先检查编译产物是否存在
        X86_OUTPUT_DIR="fanchmwrt/bin/targets/x86/64"
        if [ -d "$X86_OUTPUT_DIR" ] && [ -n "$(ls -A $X86_OUTPUT_DIR)" ]; then
          # 确保目标目录存在
          mkdir -p $GITHUB_WORKSPACE/firmware/x86_64
          # 复制所有固件文件（包括img.gz、bin、sha256sums等）
          cp -r $X86_OUTPUT_DIR/* $GITHUB_WORKSPACE/firmware/x86_64/
          echo "✅ X86/64固件已备份到：$GITHUB_WORKSPACE/firmware/x86_64"
          # 列出备份的文件，方便排查
          ls -l $GITHUB_WORKSPACE/firmware/x86_64/
        else
          echo "⚠️ X86/64编译产物目录为空或不存在：$X86_OUTPUT_DIR，跳过备份"
        fi

    # ===================== 第二步：编译AX3600固件 =====================
    - name: 清理编译环境(清理X86/64产物，避免冲突)
      if: steps.compile_x86.outputs.status == 'success' && !cancelled()
      run: |
        cd fanchmwrt
        make clean && make dirclean
        rm -f .config
        echo "✅ 编译环境已清理，准备编译AX3600"

    - name: 加载AX3600自定义配置(Load AX3600 config)
      run: |
        [ -e $CONFIG_FILE_AX3600 ] && mv $CONFIG_FILE_AX3600 fanchmwrt/.config || echo "⚠️ AX3600配置文件不存在，使用默认配置"
        # 再次给diy-part2.sh加权限（防止清理环境时丢失）
        if [ -e $DIY_P2_SH ]; then
          chmod +x $DIY_P2_SH
          echo "✅ 再次给$DIY_P2_SH添加可执行权限"
        else
          echo "⚠️ $DIY_P2_SH文件不存在，跳过权限设置"
        fi
        cd fanchmwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: 提取AX3600固件默认管理IP
      id: extract_ip_ax3600
      run: |
        cd fanchmwrt
        MANAGE_IP_AX3600=""
        CONFIG_GENERATE="package/base-files/files/bin/config_generate"
        if [ -f "$CONFIG_GENERATE" ]; then
          MANAGE_IP_AX3600=$(grep -oP '192\.168\.\d+\.\d+' "$CONFIG_GENERATE" | head -1)
        fi
        if [ -z "$MANAGE_IP_AX3600" ] && [ -f "files/etc/config/network" ]; then
          MANAGE_IP_AX3600=$(grep -A 10 'config interface 'lan'' "files/etc/config/network" | grep 'option ipaddr' | sed -r 's/.*ipaddr[[:space:]]*"(.*)"/\1/' | head -1)
        fi
        MANAGE_IP_AX3600=${MANAGE_IP_AX3600:-"192.168.1.1"}
        echo "MANAGE_IP_AX3600=$MANAGE_IP_AX3600" >> $GITHUB_ENV
        echo "✅ AX3600管理IP：$MANAGE_IP_AX3600"

    - name: 下载AX3600软件包(Download AX3600 package)
      run: |
        cd fanchmwrt
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 记录AX3600编译开始时间
      id: start_ax3600
      run: |
        COMPILE_START_AX3600=$(date +%s)
        echo "COMPILE_START_AX3600=$COMPILE_START_AX3600" >> $GITHUB_ENV
        echo "✅ AX3600编译开始时间戳：$COMPILE_START_AX3600"

    - name: 编译AX3600固件(Compile AX3600 firmware)
      id: compile_ax3600
      run: |
        cd fanchmwrt
        echo -e "$(nproc) thread compile AX3600"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        
        # 提取AX3600设备信息
        DEVICE_AX3600=$(grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/')
        ARCH_AX3600=$(grep '^CONFIG_TARGET_ARCH_PACKAGES=' .config | sed -r 's/.*="(.*)"/\1/')
        FULL_DEVICE_AX3600="${DEVICE_AX3600} ${ARCH_AX3600}"
        echo "FULL_DEVICE_AX3600=$FULL_DEVICE_AX3600" >> $GITHUB_ENV
        echo "✅ AX3600设备信息：$FULL_DEVICE_AX3600"

    - name: 计算AX3600编译时长
      id: calc_ax3600_duration
      if: steps.compile_ax3600.outputs.status == 'success' && !cancelled()
      run: |
        COMPILE_END_AX3600=$(date +%s)
        DURATION_SEC_AX3600=$((COMPILE_END_AX3600 - COMPILE_START_AX3600))
        HOURS_AX3600=$((DURATION_SEC_AX3600 / 3600))
        MINUTES_AX3600=$(((DURATION_SEC_AX3600 % 3600) / 60))
        SECONDS_AX3600=$((DURATION_SEC_AX3600 % 60))
        COMPILE_DURATION_AX3600=$(printf "%02d:%02d:%02d" $HOURS_AX3600 $MINUTES_AX3600 $SECONDS_AX3600)
        echo "COMPILE_DURATION_AX3600=$COMPILE_DURATION_AX3600" >> $GITHUB_ENV
        echo "✅ AX3600编译耗时：$COMPILE_DURATION_AX3600"

    - name: 备份AX3600固件(Backup AX3600 firmware)
      if: steps.compile_ax3600.outputs.status == 'success' && !cancelled()
      run: |
        # 先检查编译产物是否存在
        AX3600_OUTPUT_DIR="fanchmwrt/bin/targets/ipq807x/generic"
        if [ -d "$AX3600_OUTPUT_DIR" ] && [ -n "$(ls -A $AX3600_OUTPUT_DIR)" ]; then
          # 确保目标目录存在
          mkdir -p $GITHUB_WORKSPACE/firmware/ax3600
          # 复制所有固件文件
          cp -r $AX3600_OUTPUT_DIR/* $GITHUB_WORKSPACE/firmware/ax3600/
          echo "✅ AX3600固件已备份到：$GITHUB_WORKSPACE/firmware/ax3600"
          # 列出备份的文件，方便排查
          ls -l $GITHUB_WORKSPACE/firmware/ax3600/
        else
          echo "⚠️ AX3600编译产物目录为空或不存在：$AX3600_OUTPUT_DIR，跳过备份"
        fi

    # ===================== 通知与上传 =====================
    - name: 编译成功邮件通知
      if: (steps.compile_x86.outputs.status == 'success' && steps.compile_ax3600.outputs.status == 'success') && env.UPLOAD_RELEASE == 'true' && !cancelled()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.qq.com
        server_port: 465
        username: ${{ secrets.EMAIL_USER }}
        password: ${{ secrets.EMAIL_PASS }}
        subject: FanchmWrt固件编译成功通知（X86/64 + AX3600）
        body: |
          您好，FanchmWrt双设备固件编译已成功完成！
          关键信息如下：
          1. 编译时间：${{ env.COMPILE_TIME }}
          2. X86/64编译时长：${{ env.COMPILE_DURATION_X86 }}（小时:分钟:秒）
             AX3600编译时长：${{ env.COMPILE_DURATION_AX3600 }}（小时:分钟:秒）
          3. 设备型号：
             - X86/64：${{ env.FULL_DEVICE_X86 }}
             - AX3600：${{ env.FULL_DEVICE_AX3600 }}
          4. 固件管理IP：
             - X86/64：${{ env.MANAGE_IP_X86 }}
             - AX3600：${{ env.MANAGE_IP_AX3600 }}
          5. 工作流ID：${{ github.run_id }}
          6. Release链接：https://github.com/${{ github.repository }}/releases/tag/${{ env.RELEASE_TAG }}
          7. 日志链接：https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        to: ${{ secrets.RECEIVE_EMAIL }}
        from: GitHub Actions
        secure: true

    - name: 编译失败邮件通知
      if: failure()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.qq.com
        server_port: 465
        username: ${{ secrets.EMAIL_USER }}
        password: ${{ secrets.EMAIL_PASS }}
        subject: FanchmWrt固件编译失败通知（X86/64 + AX3600）
        body: |
          您好，FanchmWrt双设备固件编译工作流运行失败！
          仓库地址：${{ github.repository }}
          工作流运行ID：${{ github.run_id }}
          日志链接：https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        to: ${{ secrets.RECEIVE_EMAIL }}
        from: GitHub Actions
        secure: true

    - name: 检查空间使用情况(Check space usage)
      if: (!cancelled())
      run: df -hT

    - name: 上传Bin目录(仅开启UPLOAD_BIN_DIR时)
      uses: actions/upload-artifact@v4
      if: env.UPLOAD_BIN_DIR == 'true' && !cancelled()
      with:
        name: fanchmwrt_bin_${{ env.FILE_DATE }}
        path: fanchmwrt/bin

    - name: 上传固件目录(Upload firmware directory)
      uses: actions/upload-artifact@v4
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      with:
        name: FanchmWrt_firmware_${{ env.FILE_DATE }}
        path: $GITHUB_WORKSPACE/firmware

    - name: 读取集成插件信息
      id: plugins
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        if [ -f "$PLUGINS_FILE" ]; then
          PLUGINS_CONTENT=$(cat "$PLUGINS_FILE" | sed ':a;N;$!ba;s/\n/\\n/g')
          echo "PLUGINS_CONTENT=$PLUGINS_CONTENT" >> $GITHUB_ENV
        else
          echo "PLUGINS_CONTENT=暂无集成插件说明（可创建 $PLUGINS_FILE 文件添加）" >> $GITHUB_ENV
        fi

    - name: 生成发布说明(Generate release note)
      id: tag
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag=${{ env.RELEASE_TAG }}" >> $GITHUB_OUTPUT
        echo "## FanchmWrt 固件编译自动发布（X86/64 + AX3600）" > release.txt
        echo "- 编译时间：${{ env.COMPILE_TIME }}" >> release.txt
        echo "- 编译时长：" >> release.txt
        echo "  - X86/64：${{ env.COMPILE_DURATION_X86 }}（小时:分钟:秒）" >> release.txt
        echo "  - AX3600：${{ env.COMPILE_DURATION_AX3600 }}（小时:分钟:秒）" >> release.txt
        echo "- 设备型号：" >> release.txt
        echo "  - X86/64：${{ env.FULL_DEVICE_X86 }}" >> release.txt
        echo "  - AX3600：${{ env.FULL_DEVICE_AX3600 }}" >> release.txt
        echo -e "\n### 🔐 默认登录信息" >> release.txt
        echo "- X86/64管理IP：${{ env.MANAGE_IP_X86 }}" >> release.txt
        echo "- AX3600管理IP：${{ env.MANAGE_IP_AX3600 }}" >> release.txt
        echo "- 登录账号：root" >> release.txt
        echo "- 登录密码：password" >> release.txt
        echo -e "\n### 📦 集成插件信息" >> release.txt
        echo "${{ env.PLUGINS_CONTENT }}" | sed 's/\\n/\n/g' >> release.txt
        echo "status=success" >> $GITHUB_OUTPUT
        
    - name: 重命名固件文件(Rename firmware file)
      if: steps.tag.outputs.status == 'success' && !cancelled()
      run: |
        # 定义固件根目录
        FIRMWARE_ROOT="$GITHUB_WORKSPACE/firmware"
        
        # ===================== 重命名X86/64固件 =====================
        X86_DIR="${FIRMWARE_ROOT}/x86_64"
        if [ -d "$X86_DIR" ]; then
          echo "🔍 检测到X86/64固件目录：$X86_DIR，开始重命名..."
          find "$X86_DIR" -name "openwrt-*.img.gz" -exec bash -c '
            for file do
              if [ -f "$file" ]; then
                new_file="${file/openwrt-/FanchmWrt-x86_64-}"
                mv "$file" "$new_file"
                echo "✅ X86/64固件重命名：$new_file"
              else
                echo "⚠️ 跳过非文件：$file"
              fi
            done
          ' bash {} + || echo "⚠️ X86/64固件重命名时无匹配文件，跳过"
        else
          echo "⚠️ X86/64固件目录不存在：$X86_DIR，跳过重命名"
        fi
        
        # ===================== 重命名AX3600固件 =====================
        AX3600_DIR="${FIRMWARE_ROOT}/ax3600"
        if [ -d "$AX3600_DIR" ]; then
          echo "🔍 检测到AX3600固件目录：$AX3600_DIR，开始重命名..."
          find "$AX3600_DIR" \( -name "openwrt-*.bin" -o -name "openwrt-*.img.gz" \) -exec bash -c '
            for file do
              if [ -f "$file" ]; then
                new_file="${file/openwrt-/FanchmWrt-ax3600-}"
                mv "$file" "$new_file"
                echo "✅ AX3600固件重命名：$new_file"
              else
                echo "⚠️ 跳过非文件：$file"
              fi
            done
          ' bash {} + || echo "⚠️ AX3600固件重命名时无匹配文件，跳过"
        else
          echo "⚠️ AX3600固件目录不存在：$AX3600_DIR，跳过重命名"
        fi
          
    - name: 上传固件到Release
      uses: softprops/action-gh-release@v1
      if: steps.tag.outputs.status == 'success' && !cancelled()
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body_path: release.txt
        files: |
          $GITHUB_WORKSPACE/firmware/x86_64/FanchmWrt-x86_64-*.img.gz
          $GITHUB_WORKSPACE/firmware/x86_64/sha256sums
          $GITHUB_WORKSPACE/firmware/ax3600/FanchmWrt-ax3600-*.bin
          $GITHUB_WORKSPACE/firmware/ax3600/FanchmWrt-ax3600-*.img.gz
          $GITHUB_WORKSPACE/firmware/ax3600/sha256sums
        overwrite: true

    - name: 删除运行的工作流(Delete workflow runs)
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 0
        keep_minimum_runs: 2

    - name: 删除旧版本(Remove old Releases)
      uses: dev-drprasad/delete-older-releases@v0.3.0
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
